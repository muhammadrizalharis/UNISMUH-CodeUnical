# Sandbox SQL: sqlite3 CLI minimal (Alpine). Build:
#   sudo -n docker build -t codeunical-sqlite:latest api/sandbox/sqlite
# Dijalankan tanpa jaringan, read-only, non-root (lihat ExecuteService).
FROM alpine:3.20
RUN apk add --no-cache sqlite
