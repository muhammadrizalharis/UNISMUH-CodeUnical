# Service GPU proctor-ai. torch/torchvision/numpy DI-REUSE dari conda env py310 (JANGAN unduh
# ulang) — venv dibuat --system-site-packages (lihat setup.sh). Sisanya dari PyPI.
# CATATAN: facenet-pytorch dipasang dengan --no-deps agar tak menarik torch 2.2.x.
facenet-pytorch
ultralytics
fastapi
uvicorn[standard]
python-multipart
opencv-python-headless
tqdm
requests

