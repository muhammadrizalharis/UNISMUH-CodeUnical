# ── Dependencies ──
node_modules/
.pnp/
.pnp.js

# ── Environment & Rahasia (JANGAN commit) ──
# TIDAK ada pengecualian untuk .env.example: berkas contoh pun mudah terisi nilai
# asli saat disalin-tempel, dan sekali ter-commit ia menetap di riwayat git.
# Dokumentasikan daftar kunci di README saja, bukan sebagai berkas .env*.
.env
.env.*
secrets/
*.key
*.pem
*.crt
*.p12

# ── Build outputs ──
dist/
build/
.next/
out/
*.tsbuildinfo
**/src/generated/

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ── Data & volume (stack mandiri) ──
data/
pgdata/
miniodata/
redisdata/
*.dump
*.sql.gz

# ── Bukti kamera / rekaman ujian (SANGAT sensitif — jangan pernah ke git) ──
recordings/
proctor-evidence/
*.webm

# ── Model weights (besar) ──
*.safetensors
*.pt
*.onnx
models/

# ── Log & OS ──
*.log
logs/
.DS_Store
Thumbs.db

# ── Editor ──
.vscode/
.idea/
*.swp

# ── Berkas aturan AI auto-generate (Next.js) ──
AGENTS.md
CLAUDE.md
